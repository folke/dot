# WiFi stability improvements for MT7925 and other problematic drivers
[device-wifi]
# Reduce aggressive background scanning which can cause firmware timeouts
wifi.scan-rand-mac-address=no

[connection]
# Reduce connection timeout to fail faster and retry sooner
connection.auth-retries=3
